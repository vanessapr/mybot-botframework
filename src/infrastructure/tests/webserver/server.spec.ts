import 'mocha';
import { expect } from 'chai';
// import { createServer } from "../../webserver/server";

describe('CreateServer', () => {
    it('Should create a restify server', () => {
        expect(true).to.equal(true);
    });
});
